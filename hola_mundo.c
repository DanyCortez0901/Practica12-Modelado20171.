//Librería estandar de entrada y salida
#include <stdio.h>

// Main del programa
int main() {
  //imprimimos en la terminal el texto deseado
  printf("porque siempre tiene que ser un \"¡Hola, mundo!\"? y no algo mas cul como \"por fin funciona\" o no se, se que es tradicion pero esta muy usado XD");
  //se regresa cero porque las funciones siempre tienen que regresar algo
  return 0;
}
